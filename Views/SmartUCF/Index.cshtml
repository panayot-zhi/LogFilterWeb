@model SmartUCF

@{
    ViewData["Title"] = "SmartUCF";
}

@section head {
    <!-- Chart JS -->
    <script src="~/lib/Chart.js/Chart.min.js"></script>
}

@section sidebar {
    <li class="active">
        <a href="@Url.Action("Index", "Home")">
            <i class="now-ui-icons arrows-1_minimal-left"></i>
            <p>SmartUCF</p>
        </a>
    </li>

    <li class="active-pro">
        <a href="~/swagger" target="_blank">
            <i class="now-ui-icons files_paper"></i>
            <p>Swagger</p>
        </a>
    </li>
}

@section navbar {

    <div class="navbar-wrapper">
        <div class="navbar-toggle">
            <button type="button" class="navbar-toggler">
                <span class="navbar-toggler-bar bar1"></span>
                <span class="navbar-toggler-bar bar2"></span>
                <span class="navbar-toggler-bar bar3"></span>
            </button>
        </div>
        <a class="navbar-brand" href="#">Dashboard</a>
    </div>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navigation" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-bar navbar-kebab"></span>
        <span class="navbar-toggler-bar navbar-kebab"></span>
        <span class="navbar-toggler-bar navbar-kebab"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navigation">

        <form>
            <div class="input-group no-border">
                <input type="text" name="daterange" class="form-control" />
                <div class="input-group-append">
                    <div class="input-group-text">
                        <i class="now-ui-icons ui-1_calendar-60"></i>
                    </div>
                </div>
            </div>
        </form>

        <ul class="navbar-nav">

            <li class="nav-item">
                <a class="nav-link" href="#" onclick="window.location.reload(true)">
                    <i class="now-ui-icons arrows-1_refresh-69"></i>
                    <p>
                        <span class="d-lg-none d-md-block">Refresh</span>
                    </p>
                </a>
            </li>

            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="now-ui-icons ui-2_settings-90"></i>
                    <p>
                        <span class="d-lg-none d-md-block">Some Actions</span>
                    </p>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="#">Action</a>
                    <a class="dropdown-item" href="#">Another action</a>
                    <a class="dropdown-item" href="#">Something else here</a>
                </div>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="#">
                    <i class="now-ui-icons users_single-02"></i>
                    <p>
                        <span class="d-lg-none d-md-block">User</span>
                    </p>
                </a>
            </li>

        </ul>
    </div>

}

@*<div class="panel-header panel-header-lg">
    <div id="headerDashboardChartLoader">
        <div class="horizontalCirclesLoader">Зареждане...</div>
    </div>
    <canvas id="headerDashboardChart"></canvas>
</div>*@

<div class="panel-header">
    <div class="header text-center">
        <h2 class="title">SmartUCF</h2>
        <p class="category">TODO</p>
    </div>
</div>

<div class="content">
    <div class="row">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header ">
                    <h5 class="card-category">General analysis</h5>
                    <h4 class="card-title">Lists usage</h4>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table id="listsUsageTable" class="table table-striped table-hover">
                            <thead class="text-primary text-right">
                                <tr>
                                    <th class="text-right" style="width: 5%">
                                        view
                                    </th>
                                    <th class="text-center" style="width: 5%">
                                        #
                                    </th>
                                    <th class="text-left" style="width: 40%">
                                        listName
                                    </th>
                                    <th class="text-right" style="width: 10%">
                                        total Rows Retrieved
                                    </th>
                                    <th class="text-right" style="width: 10%">
                                        total<br>Records
                                    </th>
                                    <th style="width: 10%">
                                        avg<br>Rows Retrieved
                                    </th>
                                    <th style="width: 10%">
                                        avg<br>Retrieve Time (ms)
                                    </th>
                                    <th style="width: 10%">
                                        max<br>Retrieve Time (ms)
                                    </th>
                                </tr>
                            </thead>
                            <tbody class="text-right">
                            <tr id="listsUsageTableLoader">
                                <td colspan="8">
                                    <div class="verticalBarsLoader text-primary">Зареждане...</div>
                                </td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="card-footer ">
                    <hr>
                    <div class="stats">
                        <i class="now-ui-icons ui-1_calendar-60"></i> @Model.StartDate.ToString("dd.MM.yyyy") - @Model.EndDate.ToString("dd.MM.yyyy")
                    </div>
                </div>
            </div>
        </div>
        @*
        <div class="col-lg-12">
            <div class="card card-chart">
                <div class="card-header">
                    <h5 class="card-category">Global Sales</h5>
                    <h4 class="card-title">Shipped Products</h4>
                    <div class="dropdown">
                        <button type="button" class="btn btn-round btn-outline-default dropdown-toggle btn-icon no-caret" data-toggle="dropdown">
                            <i class="now-ui-icons loader_gear"></i>
                        </button>
                        <div class="dropdown-menu dropdown-menu-right">
                            <a class="dropdown-item" href="@Url.Action("GetStopwatchListData")" target="_blank">Get JSON</a>
                            <a class="dropdown-item" href="#">Get Stopwatch ZIP</a>
                            <a class="dropdown-item text-danger" href="#">Remove Data</a>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    <div id="pieChartLoader">
                        <div class="verticalBarsLoader text-primary">Зареждане...</div>
                    </div>
                    <div class="chart-area" style="height: auto;">
                        <canvas id="pieChart"></canvas>
                    </div>
                </div>
                <div class="card-footer">
                    <div class="stats">
                        <i class="now-ui-icons arrows-1_refresh-69"></i> Just Updated
                    </div>
                </div>
            </div>
        </div>
        *@
    </div>
</div>

@section footer {
    @* no-op *@
}

@section scripts {
    <script>
        function initializeTable(data) {

            var table = $(`#listsUsageTable`);
            var tableBody = table.children('tbody');
            var loaderRow = table.find("#listsUsageTableLoader");

            if (!data) {

                loaderRow.show();

                $.ajax({
                    url: '@Url.Action("GetStopwatchListData")',
                    success: function(r) {
                        table.trigger('data-bound.table.now-ui', [r]);
                        initializeTable(r);
                    }
                });

                return;
            }

            loaderRow.hide();
            loaderRow.nextAll().remove();

            var displayInCharts = true;
            for (var i = 0; i < data.length; i++) {
                var listData = data[i];

                if (i > 10) {
                    displayInCharts = false;
                }

                // row template
                tableBody.append(`<tr>
                                <td class="text-center">
                                    <div class="form-check">
                                        <label class="form-check-label">
                                            <input class="form-check-input" type="checkbox" ${displayInCharts ? "checked" : ""}>
                                            <span class="form-check-sign"></span>
                                        </label>
                                    </div>
                                </td>
                                <td class="text-center">
                                    ${i + 1}
                                </td>
                                <td class="text-left">
                                    ${listData.listName}
                                </td>
                                <td>
                                    ${parseFloat(listData.totalRowsRetrieved).toLocaleString('bg')}
                                </td>
                                <td>
                                    ${parseFloat(listData.totalRecords).toLocaleString('bg')}
                                </td>
                                <td>
                                    ${Math.round(listData.avgRowsRetrieved)}
                                </td>
                                <td>
                                    ${parseFloat(listData.avgRetrieveTime).toFixed(2)}
                                </td>
                                <td>
                                    ${listData.maxRetrieveTime}
                                </td>
                            </tr>`);
            }

            var sumRowsRetrieved = data.reduce((x, { totalRowsRetrieved }) => x + totalRowsRetrieved, 0);
            var sumRecords = data.reduce((x, { totalRecords }) => x + totalRecords, 0);
            var avgAllRows = Math.round(sumRowsRetrieved / sumRecords);

            // total row
            tableBody.append(`<tr class="info">                                
                                <td>
                                   
                                </td>
                                <td>
                                   
                                </td>
                                <td class="text-left">
                                   <b>Total / Average:</b> 
                                </td>
                                <td>
                                    ${parseFloat(sumRowsRetrieved).toLocaleString('bg')}
                                </td>
                                <td>
                                    ${parseFloat(sumRecords).toLocaleString('bg')}
                                </td>
                                <td>
                                    ${Math.round(avgAllRows)}
                                </td>
                                <td>
                                    
                                </td>
                                <td>
                                    
                                </td>
                            </tr>`);

            table.trigger('constructed.table.now-ui', [data]);
        }

        function initializeDateRangePicker() {

            $('input[name="daterange"]').daterangepicker({
                    "startDate": "@Model.StartDate.ToString("dd.MM.yyyy")",
                    "endDate": "@Model.EndDate.ToString("dd.MM.yyyy")",
                    "opens": "center",
                    "locale": {
                        "format": "DD.MM.YYYY",
                        "firstDay": 1
                    }
                },
                function(start, end) {

                    //console.log("A new date selection was made: " + start.format('YYYY-MM-DD') + ' to ' + end.format('YYYY-MM-DD'));

                    var jsonCookieData = {};
                    var smartUCFConfigCookieName = '@Constants.SmartUCFConfigCookieName';
                    var cookieData = Cookies.get(smartUCFConfigCookieName);

                    if (cookieData) {
                        jsonCookieData = JSON.parse(cookieData);
                    }

                    jsonCookieData.startDate = start.toDate();
                    jsonCookieData.endDate = end.toDate();

                    Cookies.set(smartUCFConfigCookieName, JSON.stringify(jsonCookieData));

                    window.location.reload(true);

                });
        }

        /*function initializeHeaderChart() {

            var availableColors = ["#3366cc", "#dc3912", "#ff9900", "#109618", "#990099", "#0099c6", "#dd4477", "#66aa00", "#b82e2e", "#316395", "#3366cc", "#994499", "#22aa99", "#aaaa11", "#6633cc", "#e67300", "#8b0707", "#651067", "#329262", "#5574a6", "#3b3eac", "#b77322", "#16d620", "#b91383", "#f4359e", "#9c5935", "#a9c413", "#2a778d", "#668d1c", "#bea413", "#0c5922", "#743411"];

            function constructChart(data, options) {

                var ctx = document.getElementById("pieChart").getContext("2d");
                var fill = drawBlueCanvasBackground(ctx);

                window.myPieChart = new Chart(ctx,
                    {
                        type: 'pie',
                        data: data
                    });
            }

            function responseToData(r) {

                var total = r.reduce((a, { totalRowsRetrieved }) => a + totalRowsRetrieved, 0);
                console.log(total);

                const thresholdPercent = 0.5;

                let labels = [];
                let datasets = [];
                let colors = [];

                for (var i = 0; i < r.length; i++) {
                    var percentage = r[i].totalRowsRetrieved / total * 100;
                    if (percentage < thresholdPercent) {
                        break;
                    }

                    labels.push(r[i].listName);
                    datasets.push(r[i].totalRowsRetrieved);
                    colors.push(availableColors[i]);
                }

                let displayTotal = datasets.reduce((a, b) => a + b, 0);
                let leftOutTotal = total - displayTotal;

                console.log(total, displayTotal, leftOutTotal);

                labels.push(`< ${thresholdPercent}%`);
                datasets.push(leftOutTotal);
                colors.push(chartColors.grey);

                let data = {
                    datasets: [
                        {
                            data: datasets,
                            backgroundColor: colors
                        }
                    ],

                    labels: labels
                }

                $("#pieChartLoader").hide();

                constructChart(data);
            }

            $.ajax({
                url: 'Url.Action("GetStopwatchListData")',
                success: function(r) {
                    console.log(r);
                    responseToData(r);
                }
            });
        };*/

        $(document).ready(function() {
            initializeDateRangePicker();
            initializeTable();
        });
    </script>
}
